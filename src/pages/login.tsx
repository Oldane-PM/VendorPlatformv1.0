export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background">
      <div className="max-w-md w-full p-6 bg-card rounded-lg border border-border shadow-sm">
        <h1 className="text-2xl font-semibold text-foreground mb-4">Sign in</h1>
        <p className="text-muted-foreground text-sm">Login form placeholder. Connect your auth provider.</p>
      </div>
    </div>
  );
}
