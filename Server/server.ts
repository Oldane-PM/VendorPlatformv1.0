/**
 * Standalone server entry (e.g. for workers, cron, or separate API).
 * Next.js API routes live under src/pages/api/.
 */

export async function start() {
  // Optional: start background workers, cron, etc.
}
