[build]
# Use Dockerfile so Node 20 is guaranteed (Nixpacks was still selecting Node 18 on some builds).
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
